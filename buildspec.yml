version: 0.2
phases:
  build:
    commands:
      - echo Entering build phase...
      - sudo npm install -g lerna
      - sudo npm install
      - yarn install
      - lerna bootstrap
      - yarn transpile
      - yarn storybook 
